# Backend Service Requirements - Pinned for Compatibility
# Core FastAPI and web framework
fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.24.0,<0.40.0
python-multipart>=0.0.6
python-dotenv>=1.0.0
starlette>=0.37.0,<0.42.0

# HTTP client for Ollama and external APIs
httpx>=0.27.0,<0.29.0
httpcore==1.0.9
aiohttp>=3.9.0         # Async HTTP for embedding_client

# Database and vector storage
chromadb>=1.0.0,<2.0.0  # v0.2.10: Aligned with roampal-core, auto-persists deletes

# Scientific computing (pinned for compatibility)
numpy>=1.22.5
scipy>=1.11.4,<2.0.0
pandas>=1.5.3,<3.0.0

# Async support
aiofiles==23.2.1
filelock==3.13.1
# anyio pulled transitively by fastapi + mcp (mcp requires >=4.5).

# Configuration and environment
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0

# Logging and utilities
loguru==0.7.2
PyYAML>=6.0.0          # YAML parsing for personality configs

# Template engine
Jinja2>=3.1.0          # Used by prompt_engine for template rendering

# Machine Learning — ONNX Runtime (v0.3.1: replaced PyTorch)
onnxruntime>=1.14.0
tokenizers>=0.15.0,<1.0.0
tiktoken>=0.5.0        # Token counting for context limits

# Text processing
nltk==3.8.1
rank-bm25==0.2.2        # BM25 for hybrid search (vector + lexical)

# Document processing
python-docx==1.1.0      # Word document processing (.docx)
pypdf>=4.0.0            # PDF text extraction (BSD, replaces PyPDF2 + PyMuPDF)
charset-normalizer>=3.0.0  # Encoding detection (MIT, replaces chardet LGPL)
beautifulsoup4==4.12.2  # HTML/XML text extraction
striprtf==0.0.26        # RTF document processing
openpyxl==3.1.2         # Excel file processing

# LangChain for advanced document processing (SmartBookProcessor)
langchain==0.3.27
langchain-community==0.3.31
langchain-core==0.3.79
langchain-text-splitters==0.3.11
aiosqlite==0.20.0       # Required by SmartBookProcessor

# Network and utilities
sniffio==1.3.0

# MCP (Model Context Protocol) Server
mcp>=1.0.0             # MCP server SDK for Claude Code integration

# HuggingFace for GGUF model downloads
huggingface-hub>=0.20.0,<1.0.0
